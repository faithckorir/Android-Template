<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.ekenya.rnd.loans">

    <dist:module
        dist:instant="false"
        dist:title="@string/title_loans">
        <dist:delivery>
            <dist:on-demand />
        </dist:delivery>

        <dist:fusing dist:include="true" />
    </dist:module>

    <application>
        <activity
            android:name="com.ekenya.rnd.baseapp.loans.MainActivity"
            android:label="@string/title_activity_loans"
            android:theme="@style/Theme.DynamicAppDemo.NoActionBar"></activity>
    </application>

</manifest>